language: cpp
addons:
  apt:
    packages:
      - libboost-program-options-dev
      - libboost-dev
script:
  - mkdir build
  - cd build
  - cmake ..
  - make && make test
compiler:
  - clang
  - gcc
