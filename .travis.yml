dist: trusty
sudo: false
language: cpp
addons:
  apt:
    packages:
      - libboost-program-options-dev
      - libboost-dev
      - libboost-system-dev
      - libboost-thread-dev
      - libboost-log-dev
      - libbyaml-cpp-dev
script: make && make test
compiler:
  - clang
  - gcc
